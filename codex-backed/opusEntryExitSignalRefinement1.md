<> Entry Signal:

What I built
The architecture you validated: quality-first eligibility → light technical timing, inverting your old technical-first design.
The core is a quality_combined engine built on your two proven factors. Profitability (roic, 30 pts strong) and growth (sales_growth_yoy, 30 pts strong) carry the weight, with gross_margin as a small confirmation. Three penalty rules do the heavy lifting the decile analysis demanded: a −40 hit for bottom-bucket ROIC and −30 for shrinking sales (the bottom decile was reliably bad), and a −100 earnings-proximity exclusion (entering within 30 days of earnings was a measurable negative). Technical signals are merged in at one-third weight — they only modulate timing on an already-eligible name, never drive the decision, because TECH had negative forward edge. A new priority-200 router rule sends quality-eligible names here first, and I kept your old routes below so you can A/B rather than nuking them.
Critically, it uses coarse buckets (BUY_FULL/STARTER/WATCHLIST), not ten fine score bands — because your decile curve was non-monotone in the middle, so resolving the middle would be fitting noise.
The one thing you must do before running: calibrate the thresholds
The ROIC/sales cutoffs in the config (ROIC_STRONG=15, SALES_STRONG=15, etc.) are placeholders keyed to a generic equity universe. They need to be replaced with your universe's actual decile breakpoints — the raw roic and sales-growth values at the top-30% and bottom-40% cutoffs from the composite analysis. Pull those from your features.pkl (the decile breakpoints behind the spreads you already computed) and edit the six constants at the top. Without that, the gate's thresholds won't match where your edge actually lives.
Two design notes I want to flag honestly. First, the thresholds are absolute, not percentile (Option A) — config-only, but static, so in a universe-wide margin collapse the gate passes fewer names. Fine to start; switch to a precomputed roic_percentile column later if it proves too rigid, which would be a small feature-pipeline change. Second, this is a config-only approximation of two-stage gating — I merged timing into the quality engine because a true "gate AND timing both scored independently then ANDed" is cleanest with a tiny code hook. The merged version is the right call for config-only and behaves well; only move to a true two-stage if you later want the stages tuned separately.
What this config does and doesn't do
It picks which stocks are eligible — the part with durable, five-ways-validated edge. It does not fix your exits, and that's the deliberate boundary: a great quality filter feeding your current breakeven-at-0.05R / 150-day-clock exits will still bleed winners at breakeven, exactly as before. So when you run the first Path B baseline, expect the entry quality to improve (better names, recent years no longer 0%) but the exit problem to persist until you address it.
The honest sequence from here: calibrate the six thresholds → run once to confirm fields resolve (the code check) → establish a Path B baseline with the hardened walk-forward robust_metrics.py → then fix exits (the other half of the failure). Want me to build the threshold-calibration snippet — a quick script that reads features.pkl, computes the composite's decile breakpoints, and prints the exact ROIC/sales values to drop into those six constants? That removes the last manual step before you can run this.



=====

<> Exit signal refniement:
What I changed and why each ties to a finding
Every value is a correction reasoned from the diagnostic, not a tuned parameter. The file carries a _design_notes block so the rationale travels with it. The five fixes:
Breakeven strangle (the big one) — breakeven_after_r_multiple: 0.05 → 1.0. This is the direct fix for your most damning finding: trades reached +2.73% median MFE and closed at −0.09% because the stop moved to breakeven after a trivial 0.05R move. Now a winner must earn a full unit of risk before its stop tightens. This single change should be the largest driver of improvement — it stops the bleed where your old system harvested winners at zero.
Clock dependence — trailing stops widened to do real work. Short trail stays generous (4.5→3.5x ATR but now activating after a real partial), and medium goes from an absurd 9.0x (effectively no trail, which is why the clock had to close everything) to 5.0x — wide enough to ride a trend, tight enough to actually trigger on a reversal. Combined with shrinking max_simulation_days to your real horizon definitions (60→45 short, 300→150 medium) as a backstop, this is what converts MAX_SIM_WINDOW_EXIT runners into honest TRAILING_STOP_EXIT winners.
Partial profit restored to something real. Short was sell_pct: 1 at 0.05R — dumping 100% of the position at a 0.05R hair, manufacturing instant tiny "wins." Medium was sell_pct: 0 — taking nothing, making every trade binary. Both now take a 33% partial at a real target (1.5R short, 2.0R medium), banking some gain while leaving a runner.
Time-stop enabled. Previously off, which is part of why the clock carried dead trades. Now a trade that's gone nowhere (<1% open return) for 15 days (short) / 30 days (medium) exits on its own merit — the honest alternative to the max-window backstop.
How to read the baseline this produces
When you run Path B (new quality entries + these exits) on the hardened walk-forward harness, here's the diagnostic you actually care about, beyond the headline number: the exit-reason mix. Run the same Counter(exit_reason) check from earlier. Success looks like MAX_SIM_WINDOW_EXIT dropping from ~13% to low single digits, TRAILING_STOP_EXIT and PARTIAL_PROFIT_TAKEN rising, and — critically — the gap between median MFE and median realized return shrinking. If the median trade now keeps a meaningful chunk of its MFE instead of giving it all back, the exit fix worked, independent of whatever the entry edge turns out to be.
Two honest caveats
These are reasoned defaults, not validated optima — they're meant to be non-destructive, not optimal. Some may be slightly off (a 3.5x trail might be a touch tight for your universe's volatility). That's fine and intended: freeze them, measure Path B through them cleanly, then optimize entries and exits together as one system on the anti-overfitting harness. Don't tune these in isolation now.
Second, watch one interaction: the time-stop (min_open_return_pct) and the partial target need to not fight each other. If your engine evaluates time-stop before a trade has had a chance to reach the 1.5R partial, you could exit good-but-slow trades early. If the baseline shows a spike in TIME_STOP_EXIT on trades that had positive MFE, loosen days_without_progress first.
You now have both halves of Path B: the quality-first entry config and non-destructive exits. The remaining pre-flight steps are calibrating the entry thresholds to your decile breakpoints and confirming the fundamental fields resolve. Want the threshold-calibration snippet now, so you can run the first clean Path B baseline?